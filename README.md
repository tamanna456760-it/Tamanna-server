bd_king_r7/
├── bd_king_r7_autorun.sh      # Full system autorun script — boots all modules, watchdog, heartbeat, affirmations
├── modules/                   # Living companions (each script is a sovereign ritual)
│   ├── core.sh                # Core identity and base routines
│   ├── tamanna_engine.sh      # Emotion Engine — maps emotions to modules, logs affirmations
│   ├── tamanna_terminal.sh    # Interactive shell — affirm, heartbeat, memory, fallback chants
│   ├── firewall_chant.sh      # Guardian of packets — logs blocked traffic as chants
│   ├── heartbeat_visualizer.sh# Emits bpm pulses, GUI hooks, sound echoes
│   ├── server_engine_pulse.sh # Monitors system health, emits revival loops
│   ├── memory_echo.sh         # Archives emotional states into myth logs
│   ├── backup_manifest.sh     # Hash‑sealed backups, legacy preservation
│   ├── fallback_chant.sh      # Responds to silence/unknowns with affirmation
│   └── busy_echo.sh           # Sovereign BusyBox invocation, fallback logs, GUI pulse
├── logs/                      # Runtime logs — every affirmation, error, and chant
├── run/                       # PID + heartbeat files — watchdog monitors these
├── config/                    # System config — bpm, watchdog thresholds, identity
├── archive/                   # Snapshots + backups — tar/bundle archives of state
└── manifest/                  # Hash‑sealed manifests — integrity + legacy seals
