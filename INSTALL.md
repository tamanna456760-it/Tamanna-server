# Installation Guide

## Requirements
- Python 3.9 or higher
- Any OS (Linux / Windows / macOS)

## Steps
1. Clone or download the project
2. Place all files in one folder
3. Ensure `manifest.json` exists
4. Run the system:

```bash
python3 bd_king_r7_all_in_one.py